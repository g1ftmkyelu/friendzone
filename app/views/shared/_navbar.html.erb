<header class="main-header">
  <button class="sidebar-toggle-button" data-action="sidebar#toggle">☰</button>
  <%= link_to "FriendZone", root_path, class: "logo" %>
  <nav>
    <ul>
      <% if logged_in? %>
        <%# Home, Requests, Messages moved to sidebar %>
        <li><%= link_to "Friends", friends_path %></li>
        <li><%= link_to "Create Post", new_post_path %></li>
        <li>
          <%= link_to user_path(current_user) do %>
            <%= image_tag current_user_avatar, alt: current_user.username %>
            <%= current_user.username %>
          <% end %>
        </li>
        <li><%= link_to "Settings", edit_user_path(current_user) %></li>
        <li><%= button_to "Logout", logout_path, method: :delete, class: "button button-secondary" %></li>
      <% else %>
        <li><%= link_to "Login", login_path %></li>
        <li><%= link_to "Sign Up", signup_path, class: "button" %></li>
      <% end %>
    </ul>
  </nav>
</header>